{
  "kind": "collectionType",
  "collectionName": "partner_ships",
  "info": {
    "name": "PartnerShip"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true,
      "unique": true,
      "minLength": 2
    },
    "activity": {
      "model": "activity",
      "via": "partner_ships"
    },
    "level": {
      "type": "integer",
      "required": true
    },
    "organization": {
      "model": "organization"
    },
    "type": {
      "type": "enumeration",
      "enum": [
        "sponsor",
        "place",
        "media",
        "community",
        "device",
        "travel",
        "vendor"
      ],
      "required": true
    },
    "accounts": {
      "collection": "account",
      "via": "partner_ship"
    }
  }
}
